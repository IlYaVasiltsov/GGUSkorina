//#include <stdio.h>
#include <stdlib.h>
#include <math.h>   

int main(void) {

    float a,b,c,d,u,s,x,y,z;
    printf("Введите переменные x,y,z");
    scanf("%f",&x);
    scanf("%f",&y);
    scanf("%f",&z);
    a=exp pow (abs (x-y));
    b=abs (x-y) pow (x+y);
    c=atan(x);
    d=atan(z);
    u=pow (pow(x,6)+ pow(ln(y),2)),1/3);

    s=exp pow
    printf("Введите x=-0,02235");
return 0;

}
